# Local: PostgreSQL 16+ (ej. Docker: docker run -e POSTGRES_PASSWORD=postgres -p 5432:5432 -d postgres:16)
# Render: pega la Internal/External URL del servicio PostgreSQL
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/captura_partidos"
JWT_SECRET="cambiar-en-produccion-secret-muy-largo"
UPLOAD_DIR="./uploads"
PORT=3001
SEED_INVITE_SECRET=mi_secreto_para_dev
