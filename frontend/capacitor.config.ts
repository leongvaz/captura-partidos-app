import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mx.ligahub.app',
  appName: 'Captura Partidos',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;